{
  "name": "lyst-core-services",
  "version": "1.0.0",
  "description": "Core package for services and service use",
  "author": "dhusted",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "rimraf node_modules package-lock.json tsconfig.tsbuildinfo dist"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1",
    "lyst-core": "file:../core"
  },
  "devDependencies": {
    "@types/mongodb": "^4.0.7",
    "@types/node": "^22.13.10",
    "rimraf": "^6.0.1",
    "typescript": "~4.9.5"
  }
}
